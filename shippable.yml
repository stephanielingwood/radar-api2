# language setting
language: node_js

# version numbers, testing against one version of node
node_js:
    - 0.10.33

before_script:
    - ls -la /
    - chmod a+rx /root
    - ls -al /
    - sudo -u www-data -s /bin/bash -c "ls -la /root"

